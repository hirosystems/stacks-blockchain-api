## ${VERSION} (${RELEASE_DATE})

**Release Type**: ✅ OPTIONAL <!-- / ⚠️ RECOMMENDED / 🚨 REQUIRED -->
**DB Migration**: ✅ No <!-- / ⚠️ Yes — see Upgrade Notes -->

```
docker pull ghcr.io/stx-labs/stacks-blockchain-api:${VERSION}
```

<!-- Release Summary: add 1–2 sentences for feature/RECOMMENDED+ releases; delete for routine patches. -->

## What's Changed

${CHANGELOG}

<!-- ## Breaking Changes — add this section ONLY when there are breaking changes. Delete otherwise. -->

<!-- ## Upgrade Notes — add ONLY if upgrading needs action: event-replay/reindex, config changes, or downtime. Delete otherwise. -->

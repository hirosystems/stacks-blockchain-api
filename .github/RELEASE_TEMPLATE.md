## ${VERSION} (${RELEASE_DATE})

**Release Type**: ✅ OPTIONAL <!-- / ⚠️ RECOMMENDED / 🚨 REQUIRED -->
**DB Migration**: ✅ No <!-- / ⚠️ Yes — see below -->

```
docker pull ghcr.io/hirosystems/stacks-blockchain-api:${VERSION}
```

${CHANGELOG}
